# Production API runtime — hosted embeddings (no local MiniLM / torch).
# Dev/test still use requirements.txt (includes sentence-transformers + pytest).

pydantic-settings==2.3.0  # centralized env var parsing — step 0.2
structlog  # structured logging — step 0.4
langfuse==2.60.10  # LLM observability tracing — step 0.5 (v2 API: trace/span/generation)
litellm==1.89.1  # LLM provider abstraction — step 0.6 (chat + hosted embeddings)
tenacity==9.1.4  # retry logic for all external calls — step 0.6
sqlalchemy[asyncio]==2.0.51  # async engine for startup DB healthcheck — step 0.10 (full session in 1.2)
asyncpg==0.31.0  # async PostgreSQL driver — step 0.10 (full session in 1.2)
fastapi==0.137.1  # web framework — step 0.10
uvicorn[standard]==0.49.0  # ASGI server — step 0.10
alembic==1.18.4          # database migrations — step 1.3
geoalchemy2==0.20.0      # PostGIS geometry types for SQLAlchemy — step 1.3
python-jose[cryptography]==3.5.0  # JWT — step 1.6
email-validator==2.3.0   # required by Pydantic EmailStr — step 1.7a
httpx==0.28.1   # async HTTP client — step 1.7b (used for OAuth, Nominatim, OSRM)
shapely==2.1.2   # geometry construction for PostGIS (scripts / geo helpers)
qdrant-client==1.15.1     # vector search for P3 semantic retrieval — AsyncQdrantClient used throughout
langgraph==0.2.76  # P5.6 — phase-gated planner agent graph
redis==5.3.1  # P6.4 — optional REDIS_URL backends for rate limit + planner cache
